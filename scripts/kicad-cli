#!/bin/sh
# Pinned native KiCad installation. Never resolve from PATH.
set -eu

KICAD_CLI="/Applications/KiCad/KiCad.app/Contents/MacOS/kicad-cli"

if [ ! -x "$KICAD_CLI" ]; then
  echo "pcbforge: KiCad 10 CLI not found at: $KICAD_CLI" >&2
  exit 2
fi

exec "$KICAD_CLI" "$@"
