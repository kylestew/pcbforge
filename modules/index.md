# pcbforge module catalog

No modules have been published yet.

ARCHITECT agents must state that the catalog is empty and propose project-local
native schematic blocks. A module is available for reuse only after it is listed
here with a version, proven-board provenance, interface summary, and saved
schematic preview.

| Module | Version | Proven on | Interfaces | Render |
|---|---|---|---|---|

A reusable block contains native `.kicad_sch` sheets, documented hierarchical interfaces,
independent acceptance tests and sourced part facts. Preserve internal UUIDs and bind each
use through a unique native sheet instance path. Copy or instantiate blocks through the
editor; do not add a parallel circuit compiler or a complete authored netlist model.

Examples without hardware evidence are development fixtures. Do not list them as proven modules.
