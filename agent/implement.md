<!-- pcbforge-legacy-playbook -->
# Legacy IMPLEMENT playbook

IMPLEMENT and build + test were merged into the schema-14 CIRCUIT phase.
Run `pcbforge migrate-circuit-phase`, then follow `agent/circuit.md`.
